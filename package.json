{
  "name": "swc-poc",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ swc-poc",
    "create-local": "graph create --node http://localhost:8020/ swc-poc",
    "remove-local": "graph remove --node http://localhost:8020/ swc-poc",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 swc-poc"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.4",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
